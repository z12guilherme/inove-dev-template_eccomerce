generator client {
    provider        = "prisma-client-js"
    previewFeatures = ["driverAdapters"]
}

datasource db {
    provider  = "postgresql"
    url       = env("DATABASE_URL")
    directUrl = env("DIRECT_URL")
}

//  name      String
    email     String
    role      String   @default("customer") // 'admin' ou 'customer'
    image     String?


    @@map("profiles")
}

model Product {
    id          String   @id @default(uuid()) @db.Uuid
    name        String
    description String?
    category    String
    mrp         Decimal  @db.Decimal(10, 2)
    price       Decimal  @db.Decimal(10, 2)
    images      String[] @default([])
    inStock     Boolean  @default(true) @map("in_stock")
    isActive    Boolean  @default(true) @map("is_active")
    createdAt   DateTime @default(now()) @db.Timestamptz @map("created_at")

    // Relações
    reviews    Review[]

    @@map("products") @id @default(uuid()) @db.Uuid
    productISt@ cimal 
     pdatedAt DateTime @default(now()) @updatedAt @db.Timestamptz @map("updated_at")

    // Relações
    profile   Profile  @relation(fields: [userId], references: [id], onDelete: Cascade)
    product   Product  @relation(fields: [productId], references: [id], onDelete: Cascade)

    @@map("reviews")
}
   String   @id @default(uuid()) @db.Uuid
    userId    String?  @db.Uuid @map("user_id")
    nhone     Stringng
ring
    
    // Relações
    profile   Profile? @relation(fields: [userId], references: [id], onDelete: Cascade)
 de

model Coupon {
    id          String   @id @default(uuid()) @db.Uuid
    code        String   @unique
    description String?
    discount    Int      // 1 a 100 (%)
    isActive    Boolean  @default(true) @map("is_active")
    createdAt   DateTime @default(now()) @db.Timestamptz @map("created_at")

    orders      Order[]

    @@map("coupons")
}
a10, 2)
mal  @default(0) @db.Decimal(10, 2) @map("shipping_cost")

    isCouponUsed         Boolean  @default(false) @map("is_coupon_used")
    couponCode           String?  @map("coupon_code")
    couponId             String?  @db.Uuid @map("coupon_id")
    discountAmount       Decimal  @default(0) @db.Decimal(10, 2) @map("discount_amount")

    status               String   @default("pending") // pending, confirmed, shipped, delivered, cancelled

    createdAt            DateTime @default(now()) @db.Timestamptz @map("created_at")
    updatedAt            DateTime @default(now()) @updatedAt @db.Timestamptz @map("updated_at")

    // Relaçõestif

    @@map("orders")
}

model OrderItem {
    id         String   @id @default(uuid()) @db.Uuid
 
r    @relation(fields: [orderId], references: [id], onDelete: Cascade)
    product    Product  @relation(fields: [productId], references: [id], onDelete: Restrict)

    @@map("order_items")
}

model FooterSettings {
    id              String   @id @default(uuid()) @db.Uuid

    updatedAt       DateTime @default(now()) @updatedAt @db.Timestamptz @map("updated_at")

    @@


